# Secrets — never commit
.env

# WireGuard keys and peer configs — contains private keys, never commit
wireguard/

# Tailscale state — contains node keys, never commit
tailscale/

# Runtime server data volumes — too large for git
data/
sandstorm/data/
rs2vietnam/data/

# Steam auth token — contains login credentials, never commit
rs2vietnam/steamcmd-state/

# OS
.DS_Store
Thumbs.db
